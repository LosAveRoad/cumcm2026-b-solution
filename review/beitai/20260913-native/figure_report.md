# 北太天元逐图报告

本轮按用户最新要求只处理画图换图，不迁移或重跑上游实验。数据只来自项目表格、代码和正文公式；没有从图片取点或读取图片数值。

## 环境

北太天元 2025（arm64）可以启动。字符前端能够执行数值表达式，但不支持 `figure`；GUI 中可运行 `draw_eda_time_components.m` 并显示正式图4。原生“导出图片”窗口已打开，但尚未验证目标 PNG 文件写入，因此图4不计为完成，论文图片未替换。

## 状态

| 图 | 状态 | 原因 |
|---:|---|---|
| 4 | blocked | GUI 图形已生成；原生导出文件尚未通过落盘检查 |
| 13, 14 | skipped | 缺少完整 Q2 网格数据；本轮不做上游计算，也不从旧热力图读色值 |
| 19 | skipped | 缺少左右面板完整原始采样点、方向和平移记录；保留旧图 |
| 1-3, 5-12, 15-18, 20-27 | pending | 尚未完成北太天元原生导出，保留旧图 |

图4脚本：`code/beitai/figures/draw_eda_time_components.m`。来源冻结、Q1 输入恢复和图19缺失检查见同目录 `inputs/`、`qa/` 与日志。

本轮没有生成可替换的新 PNG，没有改写 `paper/figures/`，也没有重新编译覆盖 `paper/main.pdf`。
