# 安装完成后给 agent 的执行指令

请在 `/Users/seint/Desktop/CUMCM2026B-Q3-reviewed` 执行北太天元全图重绘任务。北太天元由用户下载/安装，先检查实际就绪状态。

先阅读 `BEITAI_RERUN_PLAN.md`、`review/beitai_figure_manifest.json`、`review/beitai_baseline.json`、`Q3_REVIEW.md`。以当前活动 LaTeX 的实际引用为准重新核对范围，基线为正文27张图、另4张未引用旧图。不要假设规划中的 `.m` 文件已经存在或经过实测。

用户要求：

1. 找数据文件、代码和公式作为来源。禁止从图片反推任何数据，不做OCR数值提取、曲线数字化、像素取点、人工描点、图像反拟合，也不生成相似随机点冒充原样本。
2. 无法操作或无法可靠复现的项直接跳过，记录原因，继续其他项。图19目前缺失原始采样点，除非恢复出可核验的完整生成依据，否则保留旧图并列为跳过。
3. 明确区分北太天元“重绘保存结果”和“重新计算并核验”。保存结果能绘、上游实验不能复现时，可完成重绘，但必须单列上游重算跳过。
4. 全部新论文图片必须实际由北太天元运行脚本并导出。外部工具可用于整理数据、恢复原输入、参考核验、文件管理和论文编译，不能替代北太天元绘图后冒充其输出。
5. 不运行官方模拟器或任何会消耗演练/正式机会的入口。历史演练只能使用对应原记录；不把Q3新离线数据、Q3旧演练、Q4不同策略场次混用。
6. 不重跑 `revise_paper.py`，不直接执行有缺失依赖/接口探测的 `code/shared/experiment.py`，不让旧脚本覆盖原 `results.json`。
7. 保护用户当前改动，保存运行前快照。先在独立目录完成计算、绘图和论文集成，验证后同步成功文件；同步前再次对比目标文件与快照哈希，运行期间有变化则补丁合并并重新验收，不能直接覆盖。跳过图保留原文件，最终报告明确哪些仍是旧图。

按计划 P0–P5 自主推进。先实测应用兼容性：编制方案时本机为 arm64/macOS 26.5，官网macOS支持范围列为13.3–15.7。不要假定命令行参数、全部MATLAB函数或中文导出可用；检查安装版帮助并运行最小探针。没有可靠命令行入口时使用可观察的图形界面运行 `.m` 总脚本。

建立可重复执行的总入口、逐图脚本、输入来源记录、数值核验与逐图状态。几何图等比例、热力图标清方向/单位、字体和风格统一；修正与新图片直接相关的图注表述。保留Q3审定结论，不将绘图近似用于替换严格证书。

每张图失败后继续，最后交付新版 `paper/main.pdf`、北太天元脚本、原生导出图片、运行日志、逐图报告。汇总成功重绘数/本次冻结清单的 `active_count`（当前27）、整图跳过数、各计算范围核验数，以及单独的上游实验重算成功/跳过数；对保存表重新统计不能计作上游实验复现，上游跳过也不重复计入整图跳过。说明每个跳过项的原因和需要补充的条件。未实际运行北太天元或未通过图片/论文检查的项不计完成。若软件全局不可用，交付已完成的准备和环境阻塞说明，不能宣称全图完成。
