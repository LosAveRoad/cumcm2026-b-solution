# 北太天元全图重绘与可复现计算方案

编制日期：2026-09-13。项目：`/Users/seint/Desktop/CUMCM2026B-Q3-reviewed`。核查基线提交：`3acf089`。

本文件是安装完成后的 agent 执行方案。当前已完成论文、数据、代码和软件公开信息核查；尚未生成或运行北太天元脚本，也未替换论文图片。接管提示词见 `BEITAI_AGENT_PROMPT.md`，逐图机器可读清单见 `review/beitai_figure_manifest.json`，现有文件指纹见 `review/beitai_baseline.json`。

## 1. 目标与用户约束

对当前论文实际引用的全部图片逐一处理：用北太天元重新绘制，统一视觉风格；对具备可复现输入和计算逻辑的图，在北太天元中重算相关数值，再核对论文。交付新版图片、可重复运行的脚本、重新编译的论文以及逐图完成/跳过报告。

**用户明确要求：遇到无法操作或无法可靠复现的项目，直接跳过并说明原因，继续其他项目。禁止从图片反推数据。**

执行时遵守以下界限：

- 数值来源只能是可追溯的输入文件、已有结果表、代码、公式或明确的正文参数；不使用 OCR 提取图内数字作为数据，不进行曲线数字化、像素取点、人工描点或按旧图反推参数。
- 图片可用于检查布局、字体、标注和视觉含义；图片本身不得作为计算输入。示意图的坐标和比例依据公式、正文或代码生成。
- 区分“北太天元重绘已有结果”“北太天元重新计算并绘图”“跳过”。不能把第一项报告成整个实验已在北太天元复现。
- 缺少数据时先检查清单列出的本地文件及明确的依赖。无法通过正常的数据恢复或脚本修复解决，就记录跳过；不扩展为全盘搜索或长期修复任务。
- 不运行官方模拟器，不调用 `enter/measure/clear/exit`，不消耗演练或正式测试次数。历史演练图只重绘已有记录。
- 保留 Q3 已审定的算法和证明。修改范围是绘图及其所需的离线计算、直接相关的图注/文字、可复现说明；不重做 Q3 算法迁移，不把浮点绘图核用于替换有理数证书。
- 跳过某张图时保留该图在基线论文中的原文件和引用，并在交付报告中明确“仍是原图”。成功的新图和仍保留的旧图必须可辨识地记录。

## 2. 已核实的现状

### 2.1 论文与文件范围

当前 `paper/main.pdf` 共 50 个 PDF 页面。将 `paper/main.tex` 中的 `q3-body.tex` 引入后，实际引用 **27 张不同图片**，对应图 1–27。`paper/figures/` 共 31 张 PNG，其中另有 4 张未被当前正文引用。

正文使用 27 张图是默认执行范围。未引用的 `chase_flow.png`、`covering_skeleton.png`、`q3_rehearsal_bars.png`、`q3_residual.png` 记录为 `excluded_unreferenced`，保留，不重新插入论文。若未来用户要求重做整个图片目录，再单独处理它们。接管时若实际引用变化，重新冻结活动/排除清单，后续验收分母统一使用本次清单的 `active_count`；本文的27与4均为当前基线数量。

核查时发现原始出图脚本 `plot_paper.py`、`plot_schematics.py`、`cleanup_figures.py` 仅在历史来源清单中出现，当前仓库未提供；在桌面目录的同名文件检索也未找到。因此工作方式是依据现存数据和公式编写新的北太天元绘图脚本，而不是直接启动已有绘图工程。

### 2.2 软件安装与控制路径

2026-09-13 查阅的官方信息：

| 项目 | 核实结果 | 执行含义 |
|---|---|---|
| 官网产品说明 | 支持科学计算、可视化与 `.m` 脚本 | 使用 `.m` 文件作为目标脚本；具体函数仍逐项验证 |
| 官网 macOS 下载项 | 版本 2025，更新 2026-01-23，x86_64/arm64 | 当前电脑为 arm64，应选择匹配架构的官方包 |
| 官网 macOS 适用系统 | 13.3–15.7 | 当前电脑 macOS 26.5 超出列示范围，兼容性未知，需要启动与导出实测 |
| 脚本命令行批处理 | 尚未核实具体命令和参数 | 不照搬 MATLAB 的 `-batch`、`-r` 参数 |
| 图形导出、中文字体、双轴等 | 尚未在本机北太天元中验证 | 由安装后的功能探针决定实现方式 |

官方来源：

- 下载：[北太天元下载试用](https://www.baltamatica.com/download.html)
- 脚本与产品能力：[产品介绍](https://www.baltamatica.com/product/NCS.html)
- 文档入口：[产品文档](https://www.baltamatica.com/support/downlist/20.html)
- 用户资料：[北太天元科学计算与系统仿真软件](https://www.baltamatica.com/support/document/20_0006.html)

官网信息仅代表查阅时状态，不代表已经验证本机可用，也不代表所有 MATLAB 函数兼容。用户下载并安装后，agent 检查实际版本、应用路径、架构及内置帮助。只下载了安装包而尚未完成首次启动时，先完成就绪检查；许可证、账户或必须由用户处理的系统对话框单独报告。

优先选择有文档依据且已实测成功的脚本入口。若无可靠命令行入口，则通过北太天元界面打开并运行总脚本，用程序生成文件，避免逐个手工摆图。图形界面由可用的应用控制工具操作，先观察当前界面再动作。若软件无法运行或 agent 无法控制入口，则完成仍可做的数据准备、脚本整理与检查，标记所有未运行项，交付阻塞说明；不得改用其他绘图软件冒充北太天元输出。

## 3. 逐图迁移清单

下表中的“可准备”只表示来源已定位，不代表北太天元已经运行成功。编号按当前论文图号，文件均位于 `paper/figures/`。表中数据文件简称均位于 `paper/tables/`，另有说明的除外。完整来源路径、原图尺寸、图注位置、预计脚本和跳过条件在 JSON 清单中。

| 图 | 文件 | 数据或构造依据 | 执行方法与重点 |
|---:|---|---|---|
| 1 | `framework.png` | 正文四问定义；协议常数；Q1 顶点；Q2 第二站；Q3 网点 | 公式重建四联图；先完成相关单图再拼排；保留示意放大说明，交付点为 `(800,700)` |
| 2 | `eda_radii.png` | `protocol_constants.csv` | 重算同心圆；等比例坐标，增加近场/清除范围的独立放大窗 |
| 3 | `eda_constraint_line.png` | 同上 | 对数轴绘制尺度链；分离 1000/1500/1800 m 标注 |
| 4 | `eda_time_components.png` | 同上；200 m / 5 m/s | 重新计算行进 40 s；与换台 1、检测 5、光学 3、激光 2 s 对照 |
| 5 | `eda_protocol_ranges.png` | 同上 | 对数长度图；明确接收是 `[1000,1500]` 区间，其余为定值 |
| 6 | `halfplane.png` | `code/q1q2/geom.py:wedge_halfplanes`；正文 | 公式重建楔形、前向轴和内法向；实际半角 1°，放大显示需明示 |
| 7 | `wedge_intersect.png` | Q1 正交例、`q1_example_vertices.csv` | 重算两站楔交并与顶点表对照；全景和局部放大 |
| 8 | `jung_vs_diamond.png` | 正文边长 80 m 等边三角形；Q1 正交四边形 | 构造正反例并重算圆；区分对径圆与最小包围圆 |
| 9 | `q1_example_polygon.png` | `q1_example_vertices.csv`；`code/q1q2/results.json` | 重算直径、面积和包围圆；站位 `(0,0)`、`(1000,0)`，真源 `(500,500)` |
| 10 | `q1_mc_diameter.png` | `q1_mc_sample.csv` 共 400 行；`q1_mc_meta.csv` | 北太天元重新统计并绘图；全量样本、统一分箱，覆盖 382/400 |
| 11 | `q1_jung_ratio_area.png` | 同一 400 行结果 | 重算残差 `sec_radius-diameter/2` 和面积/直径散点；名称不能误写为比值 |
| 12 | `second_station.png` | `q2_axis_compare.csv`、`q2_design_row.csv`；第二站公式 | 重建坐标；区分 690.909… 网格点、700 交付点、795 轴上角度带阈值 |
| 13 | `q2_median_D_heatmap.png` | `code/q1q2/experiment.py:q2_grid`；完整网格表缺失 | 迁移计算并导出完整网格；生成热力图；不可从原图读色值 |
| 14 | `q2_psi_heatmap.png` | 同图 13 的完整网格 | 共用同一批计算；重画交角中位数；不能假定它与直径同量纲 |
| 15 | `q2_sensitivity.png` | `q2_sensitivity_sweep.csv` 6 行；`sensitivity.py` | 先重绘保存结果；再以原 70 点输入重算核对；横向偏移和可听比例分面 |
| 16 | `q2_err_sweep.png` | `q2_sensitivity_observed.csv`；`sensitivity.py` | 0.5/1/1.5°三点重算；0/1 曲线命名为“中位直径≤40 m 指示”，不得称实测清除率 |
| 17 | `q3_nine_disk.png` | `q3-body.tex` 覆盖公式；`q3_scout_xy.csv` | 重算 9 点、ρ、τ、V、P；τ 仅为证明分段半径，不画成圆形 Voronoi 边界 |
| 18 | `q34_scout_nets.png` | `q3_scout_xy.csv` 9 点、`q4_scout_xy.csv` 37 点；格网公式 | 分开显示 Q3 网和 Q4 三角格网，均等比例；不画虚构的实际访问轨迹 |
| 19 | `q34_coverage_scatter.png` | 仅存示意图和部分采样函数，原始左右面板点集未找到 | **暂列数据缺失**；只有恢复到可追溯的原始点集/完整生成逻辑并核验后才执行，否则整图跳过 |
| 20 | `directional_listen.png` | 正文 `G=(200,0)`、东向、`a=500`；格网覆盖半径 | 公式重建半平面和覆盖盘，验证 q/p 前向与距离关系 |
| 21 | `q4_directional_sector.png` | 正文闭 180° 扇形定义；最小接收半径 | 重建前向闭半圆与朝向箭头，前后半面不混淆 |
| 22 | `q4_silent_spoke.png` | 正文 G=200、q=700、p=850；`ρ=850/√3` | 重建轴向剖面；直接标清 a 与 ρ |
| 23 | `q4_sector_chase.png` | 正文反例与 `(r,t)=(60,500)` 示意 | 依据公式构造留瓣/越界对照；不将示意点当成演练实际第二站 |
| 24 | `q4_optical_grid.png` | 方格边长 25、清除半径 20、启用限度 80 m | 重算格点及 `25/√2≈17.6777`；以完整相交网格单元保证边缘覆盖 |
| 25 | `q4_fullclear_bars.png` | `q4_fullclear.csv`；`q4_policy_compare.csv` | 只用这组旧三角格网演练，10/10、12/12、12/12；逐场平均时间按总时间/已清数核对 |
| 26 | `q4_rehearsal_bars.png` | `q4_rehearsal.csv`；`q4_policy_compare.csv` | 只用原窗口同辐条网演练，11/12、9/10、12/12；明确两次未全清 |
| 27 | `q4_ring_ablation.png` | `q4_ring_ablation.csv`；三角格公式 | 重算 `ρ=s/√3`，850 合格、900 不合格；临界值用精确 `500√3`，不是 866.03 |

从现有资料看，26 张图具备直接绘制或公式/代码恢复的依据，图 19 存在明确的数据缺口。这个数字是准备情况，最终完成数量以软件实测和验收记录为准。

## 4. 数据恢复与计算规则

### 4.1 统一的数据来源记录

每张图保存三类信息：输入来源和哈希、实际执行的计算/绘图脚本和软件版本、输出文件及核验结果。数值数据导出为 UTF-8 CSV 或纯数值文本，字段名称与单位另行记录；用至少 15–17 位有效数字保存新生成的双精度数值，显示时再四舍五入。

原有结果表是有效的重绘来源，但不能据此宣称其上游实验也重跑完成。例如 Q1 的 400 行表是样本计算结果，尚未逐行保存测站、真源和误差；Q4 演练 CSV 是现有汇总记录，不等于重跑官方实验。

### 4.2 Q1 的两层复现

第一层为必做且资料齐备的重绘：读取 400 行 CSV，在北太天元中重新计算直方图、中位数、覆盖计数、残差，输出图 10/11。

第二层是条件执行的上游计算复现：依据 `monte_carlo_two_station` 原始随机数调用顺序和 NumPy `default_rng(2026)`，在独立准备目录生成 `k,s1x,s1y,s2x,s2y,gx,gy,e1,e2`，必要时补充两个测向角；核对全部 400 行后，把冻结输入交给北太天元几何程序。Python 在这一层只负责恢复原输入或提供参考核验，最终北太天元计算需独立产出结果。

不要求北太天元的 `rng(2026)` 与 NumPy 产生同一随机流。原 NumPy 版本未冻结，应记录恢复时版本；若恢复输入不能重现已有表，第二层标记跳过，第一层仍可完成。报告写为“结果重绘完成，上游 Monte Carlo 重算未完成”，不得通过挑选随机种子或调样本凑出 382/400。

原 Q1/Q2 计算使用 96 边形近似场地、64 边形近似接收盘，以及现有半平面裁剪容差。复现时先保持这些定义，明确它们是已有数值实验口径，不把内接近似升级为严格连续覆盖证明。图中用于显示的圆可用更密的折线绘制，但不能因此悄然改变计算边界。

### 4.3 Q2 热力图与扫描

已核实的确定性输入和网格：

- 第一站 `(0,0)`，示向度 35°；半角 1°。
- 70 个位置来自 `sample_first_wedge` 的 14 个径向中点 × 5 个角向中点；径向端点为 `5+1e-6` 与 1500 m。不是随机取 70 点。
- `r=linspace(400,1400,11)`；`|t|=linspace(150,1000,12)`；正负两侧分别计算，至多 264 条参数记录。不能不经核对就将两侧平均。
- 径向/角向中点的构造公式、1500 m 接收筛选、近场筛选、少于三个顶点的过滤、`n_heard` 的定义必须照原函数核实；无有效后验的格点记缺失，不能填 0。
- 中位数基于有效后验，比例按原实现的 70 个输入为分母；P90 复现 NumPy 默认线性插值，而不是默认各软件分位数实现一致。
- 两张热力图共用一个完整网格数据文件；默认展示 `sign=+1` 面，另核对负侧差异并记录。用于对照的矩形取现存结果中的范围，不能从图上量取，也不能把矩形描述成全域保证集。
- 核对网格点 `(800,690.909090909…)` 的中位直径 48.2744924419 m、交角中位数 93.8546454622°，以及现存候选带。标记交付点 `(800,700)` 时增加局部放大或引线，使两个相近点可区分。
- 偏移扫描采用 `t=[200,350,500,650,800,1000]`、`r=1100`；误差扫描采用 `err=[0.5,1,1.5]`、`(r,t)=(1100,500)`，各半角都按代码重新生成对应的 70 点。

Q2 目标是独立迁移几何计算到北太天元。若数值核未能与保存的关键结果一致，图 13/14 暂不替换并记录差异；图 15/16 仍可依据保存结果重绘，计算状态单独标记跳过。不能从两张现有热力图恢复数值。

### 4.4 已知代码入口问题

- `code/q1q2/experiment.py` 部分函数依赖 `jianmo.b_sim`，当前目录结构没有完整安装包。相关参考实现位于 `code/src_jianmo_b_sim/geometry.py`。如需生成参考输入，采用独立、可审查的离线适配，不能直接认定入口可运行。
- `code/shared/experiment.py` 引用了缺失的 `twin`、当前目录下未提供的 `searcher`，并含官方接口探测逻辑。不要作为绘图总入口执行；`JIANMO_GATE=1` 也不能解决导入缺失。按需读取和迁移纯几何函数。
- `sensitivity.py` 会向进程当前目录写 `results.json`；与其他旧脚本一样，需要复制/适配到独立运行目录后再运行，避免覆盖原始结果。
- `revise_paper.py` 是一次性 Q3 结构修订脚本，不是可重复运行的绘图命令；不再次运行。
- `code/q4_method1/result.json` 记录了与图 25 的历史三场不同的策略/场次。图 25 只能绑定 `q4_fullclear.csv`，不能选一个更新的 JSON 替换成绩。

### 4.5 图 19 的跳过条件

现有代码中可以找到某些圆盘采样逻辑，但当前仓库没有保存图 19 左右面板的完整采样数据和出图脚本。`q34_observed.csv` 是统计摘要，测站坐标表也不能代替源、方向和平移点。

执行时只做一次针对现有工程来源的恢复检查。只有恢复出左右面板的确定性输入、筛选规则和生成顺序，并完成数值核验后，才取消跳过。若不能恢复，就记录 `SKIPPED_MISSING_SAMPLE_DATA`，保留原图。不要自行生成一批“看起来相同”的新点，也不只替换其中一个面板后宣称整图已重跑。

## 5. 图形设计与兼容实现

统一采用论文印刷风格：白底，主色深蓝 `#2463A0`，对照橙 `#D9822B`，辅助灰 `#737B86`，关键不合格项暗红 `#B64545`。颜色必须结合不同线型/点型，灰度输出仍可理解。对数值热力图使用单调明度的顺序色带，不依赖红绿区分。

先确定最终论文中的物理宽度，再设置字体和导出尺寸。建议按当前 `\textwidth` 实测：图内文字最终不小于约 8 pt，主线 1.0–1.4 pt，次线 0.7–1.0 pt，留出单位、色条和图例空间。中文选本机实测可导出的字体，英文、数字和数学符号保持一致；不预设某个 Windows 字体一定存在。

所有几何图使用等比例坐标；局部放大窗口明确标记范围；示意半角放大时同时说明真实参数。热力图固定行列映射、坐标方向和色条单位，不为视觉平滑对结果插值。历史柱状图标出案例序号和数值，真实数与已清数用分组柱或明确点型，避免完全重叠导致“看不出两列”。

每张图优先从北太天元导出矢量 PDF，并导出 300–600 dpi PNG 预览。矢量失败但 PNG 合格时，允许使用北太天元直接导出的高分辨率 PNG，并记录格式限制。不得把旧图截屏、转换后当作新输出。

候选兼容路径如下，具体函数名/调用方式以安装版帮助和探针结果为准：

| 能力 | 优先实现 | 兼容处理 |
|---|---|---|
| 布局 | 基础子图/坐标轴 | 手动固定轴位置，避免依赖高级布局接口 |
| 热力图 | 基础矩阵颜色图及色条 | 逐格矩形填色，使用同一矩阵和色限 |
| 几何区域 | 多边形填充、直线、文字 | 不依赖 `polyshape`；用自写半平面裁剪 |
| 透明填色 | 原生透明度 | 浅色实填，不损失边界信息 |
| 误差图两种量纲 | 双轴或两面板 | 双轴不可用则两面板，保留全部数据及单位 |
| 数据读取 | 安装版支持的文本/CSV 读取 | 预先无损整理纯数值文本和独立标签；不经 Excel 手工复制 |
| 图形导出 | 安装版支持的矢量/位图导出 | 用已验证的 GUI 导出；均失败则跳过 |

这张表是实现策略，不是对具体 API 可用性的承诺。

## 6. agent 执行阶段

### P0：基线和环境，先确认能跑

1. 阅读本文件、接管提示词和逐图清单；检查 `git status`，尊重用户已有改动。重新扫描当前活动 LaTeX 引用；与 27 图基线不符时更新清单并解释范围变化。
2. 对比基线哈希；基线不同就建立新的运行快照，不覆盖用户变化。建立 `review/beitai/<run-id>/`，记录软件、系统、脚本和输入版本。
3. 在新运行目录保留完整论文副本，后续先在副本集成。大文件备份按需复制，不能仅靠 Git 假设工作树中没有未提交改动。
4. 找到北太天元实际安装位置，完成首次启动和脚本入口检查。软件不可用则标记环境阻塞，同时继续可做的准备，不声称完成。
5. 编写并运行 `probe_environment.m`：最小数值运算、带表头数据读取、中文/希腊字母、等比例圆、对数坐标、子图、色条、柱图、PDF/PNG 导出。每项保留成功证据或错误文本，输出 `capabilities.json` 或等价日志。

### P1：输入冻结与参考数值

按清单将可用 CSV/JSON 和参数无损整理到新运行目录。缺失字段保持缺失，不补零。生成输入哈希与来源映射。需要重算的纯几何函数单独迁移，先通过正交交会、三角格、圆半径和统计口径的核对，再运行参数扫描。

图 19 按第 4.5 节执行恢复检查；无法恢复就当场报告跳过。上游 Monte Carlo 输入无法核实时，只跳过上游重算，保留由原 400 行结果重绘图 10/11 的工作。

### P2：做三类样图，建立统一设置

先完成图 4（柱状图）、图 17（几何与中文）和图 13（数据与绘图核通过时的热力图）。若图 13 计算仍未完成，可用明确标记为探针的小矩阵测试绘图能力，但该探针不是论文结果。

检查实际导出文件中的字体、尺寸、单位、线型和边距。确定兼容实现后固化 `style_defaults.m` 和 `export_figure.m`。无需每张图都问用户确认；只有缺少必须由用户处理的环境信息时集中说明。

### P3：分批执行与逐图继续

执行次序：协议尺度图 → Q1 几何/统计 → Q2 第二站/扫描/热力图 → Q3/Q4 几何 → Q4 演练统计与间距扫描 → 图 1 四联总览。

总脚本为每张图单独捕获失败，保存日志，继续下一张图。可依据错误采用表中等价兼容路径；若仍不能可靠运行，立即标记跳过，不反复尝试猜测参数。共用数值核失败时只跳过依赖它的图，不阻塞其他批次。

计划的代码结构如下，**这些 `.m` 文件尚未创建，须在安装后按探针结果实现**：

```text
code/beitai/
  README.md
  probe_environment.m
  run_all_figures.m
  run_one_figure.m
  style_defaults.m
  export_figure.m
  prepare_inputs.py             # 可选，仅用于无损整理、原输入恢复或参考核验
  geometry/                     # 楔形、裁剪、直径、最小包围圆、格网
  compute/                      # Q2 网格/扫描、Q1 条件重算
  figures/                      # 与 manifest 相对应的 draw_*.m

review/beitai/<run-id>/
  environment.json
  capabilities.json
  inputs/
  recomputed/
  figures/                      # 北太天元原生导出的 PDF/PNG
  logs/
  paper/                        # 副本集成和编译结果
  qa/
  run_status.json
  figure_report.md
```

如果 JSON 写出函数不兼容，`.m` 写可解析的 UTF-8 状态文本，再由外部工具无损整理为 JSON；外部工具不能代替北太天元绘图。

### P4：数值与图片验收

每图的执行状态与计算状态分开记录：

```text
render_status: pending | passed | skipped | failed
computation_status: pending | not_required | saved_results_only | recomputed_verified | skipped
computation_scope: none | saved_result_statistics | formula_geometry | original_experiment
upstream_recompute_status: not_applicable | pending | verified | skipped
source_status: available | recoverable | missing | excluded_unreferenced
```

`computation_scope` 表示本次实际核验的计算范围，不按预期填写。对保存结果计算中位数/柱状统计属于 `saved_result_statistics`；按公式重建几何示意属于 `formula_geometry`；恢复实验输入并重新计算后验结果才属于 `original_experiment`。不能把前两类计入上游实验复现。

`upstream_recompute_status` 单独记录已计划的上游实验计算。例如图10可同时为 `render_status=passed`、`computation_status=recomputed_verified`、`computation_scope=saved_result_statistics`、`upstream_recompute_status=skipped`：图片已重绘，统计已核对，但400次几何实验未复现。不适用上游实验的纯示意图写 `not_applicable`。

每条报告至少包含：图号、文件名、数据来源及哈希、脚本、软件版本、实际导出格式、核验指标、状态、跳过原因、旧图是否保留。`pending` 和 `failed` 不能被计入完成；处理结束时所有活动图必须归为 `passed` 或有原因的 `skipped`，失败历史保留在日志中。所有曾计划的上游计算也必须给出 `verified` 或有原因的 `skipped`，不能留成未说明的待办。

汇总分别给出 `render_passed_count`、`render_skipped_count`、各 `computation_scope` 的核验图数、`upstream_verified_count`、`upstream_skipped_count`。前两项之和须等于本次冻结的 `active_count`；上游跳过不重复计入整图跳过。另列上游计算任务数，注明同一批400次实验可服务图10/11，图数不等于独立实验批次数。

核心核对值：

| 项目 | 基线数值或关系 | 验收方式 |
|---|---|---|
| 协议尺度 | 5、20、1000、1500、1800 m | 与 CSV 完全对应；半径不误当直径 |
| 行进时间 | 200/5=40 s | 精确关系，动作分量匹配 |
| Q1 正交例 | D≈34.920769491851 m，R≈17.460384745865 m，面积≈609.358639864615 m² | 与保存顶点/结果比较 |
| Q1 统计 | 400 行；覆盖 382、未覆盖 18；中位直径≈137.950259962638 m | 行数/整数精确，中位数对照 |
| Q2 网格点 | `(800,690.909…)`；中位直径≈48.2744924419 m | 验证参数和指标，不强行选到相同点 |
| Q2 交付点 | `(800,700)`；795 是另一种角度带阈值 | 三种意义分开标注 |
| Q2 误差扫描 | 中位直径约30.9931、61.8675、92.6540 m；阈值指示1、0、0 | 不解释为三种清除概率或成功率 |
| Q3 | 9 点；ρ≈956.051088048838 m；τ≈541.196100146197 m | 解析计算；τ 不等同最近站圆分界 |
| Q4 | 37 点；s=850；ρ≈490.747728811182 m | 点集核对；`a−ρ>0` 且 `a+ρ<1000` |
| 光学方格 | 25/√2≈17.677669529664 m <20 | 同时验证 80 m 边缘相交单元覆盖 |
| 图25演练 | 真数/已清：10/10、12/12、12/12 | 从对应 CSV 计算每场时间/已清数 |
| 图26演练 | 真数/已清：12/11、10/9、12/12 | 与图25来源分离；不配对作性能因果比较 |
| 间距临界点 | s=500√3 时 ρ=500；严格条件不合格 | 不用显示舍入值866.03决定真假 |

比较全精度几何长度时建议绝对容差 `1e-6 m`、相对容差 `1e-8`；面积绝对容差 `1e-3 m²`；角度绝对容差 `1e-6°`；分位数采用相同算法后对照。读取已经舍入的输入时结合其有效位数解释误差，不能要求超过源数据精度。接近分类边界时使用原模型的容差/严格不等式检查，禁止靠放宽核验容差更改类别。

无论数值是否通过，都必须查看真实导出的全部图片和最终 PDF 中对应页面，检查中文缺字、单位、遮挡、裁切、比例、双轴/色条含义及实际字号。更改样式可能更新图注中的颜色描述；要同步修改直接相关正文和图注，而不改动不相关结论。

### P5：论文集成与交付

1. 先在 `review/beitai/<run-id>/paper/` 副本中完成图片引用、必要图注和说明更新。PNG 合格时可保持原文件名；PDF 合格时只修改清单对应的扩展名，不能全局替换所有 `.png`。
2. 图 16 把模糊的“一次清除”改为明确的中位直径阈值指示；正文“恰好卡在边界”也需与 61.8675 m 的结果一致，不宣称等于 40 m。相邻真实一次清除比例仍保持原含义。
3. 保留图号和语义映射。若图 19 跳过，其旧图和说明保留，报告列明该图仍来自原版；不能把整篇标为“所有图片均由北太天元生成”。
4. 在副本 `paper` 目录运行两次 `xelatex -interaction=nonstopmode -halt-on-error main.tex`；检查编译错误、未定义引用和影响图形的溢出。当前机器已找到 XeLaTeX，仍需以本次构建日志为准。
5. 按最终页码检查本次冻结清单中的全部活动图位（当前基线27个）以及受影响页面。实际图高变化可以改变分页，不强制保持50页；报告新页数和任何无法解决的版面问题。
6. 全部可完成项通过后，逐一比较待写回文件与 P0 快照的哈希。运行期间用户修改过的文件必须按补丁合并并重新编译/验收，不得整文件覆盖；无法安全合并则保留候选结果并报告该项集成跳过。对未变文件先备份再同步成功图片及配套文字，跳过项保留原文件。同步后核对引用、哈希及最终 PDF。若集成失败，只撤销本轮确实写入且之后未再变化的文件；再次变化的文件保留并报告冲突，不能用回滚覆盖用户新改动。
7. 如实更新与本次工作有关的软件/AI使用说明，写清北太天元的作用、Python辅助的数据恢复作用和跳过范围；不能宣称历史模拟器记录由北太天元生成。
8. 交付 `figure_report.md`、`run_status.json`、实际运行脚本、输出图片和最终 PDF。按 P4 分别汇总成功重绘数/本次 `active_count`、整图跳过数、各计算范围核验数、上游实验重算成功/跳过数，以及本次未引用旧图的排除情况；当前基线分母为27、排除数为4。

## 7. 失败时如何报告并继续

推荐原因码：

| 原因码 | 含义 | 后续动作 |
|---|---|---|
| `SKIPPED_ENVIRONMENT` | 安装版无法启动/不可控制/无脚本入口 | 保留准备成果；未运行图都不计完成 |
| `SKIPPED_MISSING_SAMPLE_DATA` | 缺少原始点集和完整生成依据 | 保留旧图，列出缺失字段和查过的来源 |
| `SKIPPED_UPSTREAM_RECOMPUTE` | 保存结果可绘，但上游实验不能可靠复现 | 可继续重绘；`upstream_recompute_status` 写跳过，不计入整图跳过 |
| `SKIPPED_NUMERICAL_MISMATCH` | 新计算与基线不一致且无法解释 | 不替换受影响结果图，报告具体差值 |
| `SKIPPED_UNSUPPORTED_GRAPHICS` | 所需绘图及等价基本图元方式均不能运行 | 保留旧图，继续下一图 |
| `SKIPPED_EXPORT_OR_LAYOUT` | 无可靠导出或版面始终不合格 | 不把截图/旧图伪装成新图 |

跳过记录应写成可行动的说明，例如：“图19：缺少左右面板的源位置、方向和平移点原数据，现有统计表无法恢复；保留原图，未进行像素取点。其余26图继续。”

用户无需逐图授权。遇到独立图的失败就说明并继续；若是全局环境问题，先完成剩余可独立完成的准备，再集中说明需要用户解决的唯一必要条件。

## 8. 接管时的最短指令

> 北太天元已经安装好。请读取本项目的 `BEITAI_AGENT_PROMPT.md` 和 `BEITAI_RERUN_PLAN.md`，按逐图清单开始执行。禁止从图片反推数据；不能可靠操作或复现的项目直接跳过，说明原因并继续。完成后给我新论文、北太天元脚本，以及成功和跳过清单。

计划不承诺未经测试的运行耗时。用 P0/P2 的启动、数值核与导出实测来评估余下工作；优先暴露会影响全部图片的环境问题，再批量推进。
